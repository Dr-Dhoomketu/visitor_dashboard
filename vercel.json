{
  "buildCommand": "pnpm --filter @workspace/vms-ui run build",
  "outputDirectory": "artifacts/vms-ui/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
